const artifact = require('');

describe('test add artifact', ()=> {
  it('contains artifact in ArrayList', ()=>{
    expec(artifact.add()).toBe()
  })
})

describe('remove add artifact', ()=> {
  it('contains artifact in ArrayList', ()=>{
    expec(artifact.remove()).toBe()
  })
})

describe('edit add artifact', ()=> {
  it('contains artifact in ArrayList', ()=>{
    expec(artifact.edit()).toBe()
  })
})
